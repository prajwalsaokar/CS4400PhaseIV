@page
@model BusinessAPI.Pages.DriverViewModel
@{
    ViewData["Title"] = "Driver View";
}

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business Supply System - Driver View</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            color: #333;
        }
        h2 {
            text-align: center;
            margin-top: 20px;
            color: #444;
        }
        .view-container {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 20px;
            margin: 20px auto;
            width: 90%;
            max-width: 1200px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #007bff;
            color: #fff;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h2>Driver View</h2>
    <div class="view-container">
        <table>
            <thead>
                <tr>
                    <th>Username</th>
                    <th>License ID</th>
                    <th>License Type</th>
                    <th>Successful Trips</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var driver in Model.Drivers)
                {
                    <tr>
                        <td>@driver.Username</td>
                        <td>@driver.LicenseID</td>
                        <td>@driver.license_type</td>
                        <td>@driver.successful_trips</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div style="text-align: center;">
        <a asp-page="/Index" class="btn">Back to Procedures</a>
    </div>
</body>